import { CheckErrorDirective } from './check-error.directive';

describe('CheckErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckErrorDirective();
    expect(directive).toBeTruthy();
  });
});
